spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://postgresql:5432/product
    username: root
    password: root

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:18800/eureka/
